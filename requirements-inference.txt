# Lightweight requirements for inference-only setup
# This file contains minimal dependencies for running inference with pre-trained models

# Core dependencies
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.19.0

# Model downloading and management
modelscope>=1.9.0

# ModelScope dependencies that are often missing
addict>=2.4.0
simplejson>=3.19.0
oss2>=2.18.0
sortedcontainers>=2.4.0
yapf>=0.33.0

# ONNX runtime for efficient inference
onnxruntime>=1.16.0

# Audio processing
soundfile>=0.10.3
librosa>=0.10.0
pydub>=0.25.1
scipy>=1.7.0

# Utilities
pyyaml>=5.4.1
tqdm>=4.61.1
requests>=2.28.0
filelock>=3.12.0
huggingface-hub>=0.16.0

# Optional: For demos
# gradio>=4.0.0
# sherpa-onnx>=1.10.0  # Optional: for ASR/TTS